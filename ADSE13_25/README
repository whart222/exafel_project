IOTA code currently performs a brute force search over the algorithm parameter space. Deliverable is the ability to reproduce current results obtained from a Linux cluster.


Examples files/scripts are provided in XFEL_regression
